<!DOCTYPE html>
<html>
<head>
  <title>{{title}}</title>
</head>
<body>

  <h1>{{title}}</h1>

  {{#each sections}}
    <section>
    
      {{#each this}}
        <h2>Name: {{name}}</h2>
        <p><strong>Description:</strong> {{description}}</p>
        <code>{{example}}</code>
      {{/each}}
    
    </section>
  {{/each}}

  {{> footer}}

</body>
</html>